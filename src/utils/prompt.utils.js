export const generatePromptBinary = (numberToConvert) => {
    return `Tu tienes un rol de convertidor binario y requiero que conviertes este numero ${numberToConvert} a binario`
}

export const generatePromptVocals = (textCount) => {
    return ` Tu tienes un rol de contar las vocales de este texto: ${textCount}`
}